import { getTrackEndpoint } from './get'
import { findTrackEndpoint } from './find'
import { createTrackEndpoint } from './create'

export const trackEndpoints = [
  getTrackEndpoint,
  findTrackEndpoint,
  createTrackEndpoint,
]
