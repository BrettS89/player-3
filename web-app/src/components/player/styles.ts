import { blue, lightGray } from '../../styles/colors';

export const styles = {
  player: {
    display: 'flex',
    height: 100,
    alignItems: 'center',
    justifyContent: 'center',
    backgroundColor: lightGray,
  },
}
